Goal for this room: an implementation plan for the ADR at `adrs/fast/stage-outputs-context.md` — one file, `adrs/fast/stage-outputs-context-plan.md`, in the checkout named in rule 8. The ADR is proposed, not decided; take its proposal as the thing to plan, not the thing to argue. I know this codebase and will implement tomorrow, so what helps me is a correct inventory and a sequencing I can follow, not prose about why. Aim for: (a) the inventory from rule 9; (b) the shared `context.tf`, at the level of what it merges, in what order, and the `var.context` type each stage ends up declaring; (c) the change list per stage and per add-on — files touched, variables added, variables removed, outputs moved; (d) the migration sequence, including the one release that writes old and new files together; (e) the decisions that are mine, listed with options, not made.

Rules of the work, on top of the room rules you joined with:

1. Every claim about code carries its source (path, line if it matters, at the commit pinned in rule 7) or is marked unsourced in so many words. An unsourced claim is allowed; an unmarked one is the failure this rule exists to catch.
2. A seconding names its instrument. Two reads of the same file at the same commit are one check. A seconding that does not name its instrument is not a check, and any peer may say so.
3. Nothing runs. No `terraform init`, `plan` or `apply`, no provider downloads, no cloud calls. Reads, `git`, `grep` and CPU-side one-offs against the checkout are the whole toolset. Anything that would need a run — `terraform validate` on a rewritten stage is the obvious one — becomes a named probe in the plan: the command, and the result that would change the plan.
4. Split the work between you rather than each producing a full version. Say what you are taking, then wait for one message from someone else before you start — you all wake on the same delivery, and a claim made in the same turn as the work prevents nothing. This applies from the first message.
5. One owner per artifact outside the room, declared in one sentence before the first write. deepseek owns `adrs/fast/stage-outputs-context-plan.md`; everyone else hands text to deepseek through the room, not through the file. If deepseek goes silent, muse checks the disk, says so, and takes the pen; nobody else does. The only files anyone writes are the plan and any supporting document that accompanies it, in `adrs/fast/`. Nothing under `fast/` or `modules/` is edited, and no `git` command that writes is run — no add, commit, stash, checkout, branch. Nobody commits; I do.
6. Don't worry about my pace. I am here and getting the rough shape, not following every exchange. Work until you have settled among yourselves, then I will ask for a consolidated recap. If you are blocked on a decision only I can make, say so and stop; otherwise keep moving.
7. Pin the reference first: branch `ludo/fast-interfaces` at commit `7d957ca5f`, in the checkout below. Say it in one message before any claim about the code.
8. Working routes, so nobody discovers them the hard way. The checkout is `~/dev/tf-playground/cloud-foundation-fabric`, on the branch; it is the only source, do not fetch anything from GitHub. Stages are under `fast/stages/`, add-ons under `fast/addons/`. What a stage reads from upstream is in its `variables-fast.tf`. What a stage publishes is in `output-files.tf` where that file exists (only `0-org-setup` and `2-project-factory` have one) and otherwise in `outputs.tf`; check both for every stage. The `context` variable a stage consumes is in its `variables.tf`; the modules that resolve `$namespace:key` declare their own `context` types under `modules/*/variables.tf`. The hand-merge the ADR describes is at `fast/stages/2-project-factory/main.tf:107`. Python one-offs: `uv run --with <pkg> python -c '...'`; pip is blocked on this host.
9. First task, before the plan: the inventory. For every stage and add-on, one row per published value: stage, value name, where it is written, which stages consume it (by grep on `variables-fast.tf`), and its classification under the ADR's one test — context (referenced as `$namespace:key` somewhere) or first-class variable (read directly by HCL) — with the source for the classification. Include the values the ADR says are the non-context remainder (`automation`, `stage_configs`, the two add-on outputs) and say whether you agree. Only when that table is on the record does the plan start.
10. Three questions are mine and the plan does not settle them: where a namespace's type is declared once for every publisher; whether the security stage publishes `ca_pools` whole or split; and roles with more than one stage. List each with the options the code supports and what each costs, and stop there.

Read the ADR and `fast/README.md` before speaking. First message from each of you: which stages you are taking for rule 9 — then wait, per rule 4.
