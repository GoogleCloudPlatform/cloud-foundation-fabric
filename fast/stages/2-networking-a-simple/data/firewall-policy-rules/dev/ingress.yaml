# skip boilerplate check

ingress-default-deny:
  description: "Deny and log any unmatched ingress traffic."
  priority: 2147483643
  action: "deny"
  match:
    source_ranges:
      - "0.0.0.0/0"
  enable_logging: true
