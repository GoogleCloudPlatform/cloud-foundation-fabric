# skip boilerplate check

allow-onprem-probes-example:
  description: "Allow traffic from onprem probes"
  ranges:
    - onprem_probes
  targets: []
  rules:
    - protocol: tcp
      ports:
        - 12345
