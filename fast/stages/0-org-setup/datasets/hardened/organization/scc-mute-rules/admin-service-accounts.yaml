admin-service-accounts:
  description: Mute admin service accounts findings except for core iac service account.
  filter: >
    category="ADMIN_SERVICE_ACCOUNT"
    AND contains(iam_bindings, member : "-prod-iac-core-0.iam.gserviceaccount.com")
  type: DYNAMIC
