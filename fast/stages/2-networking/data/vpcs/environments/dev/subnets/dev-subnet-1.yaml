# yaml-language-server: $schema=../../../../../schemas/subnet.schema.json

# Terraform Resource: google_compute_subnetwork
# Reference: $vpc_subnet_ids:dev/dev-subnet-1

region: us-central1
name: example-subnet1
network_id: $vpc_ids:environments/dev
description: "Default subnet for dev environment"
ip_cidr_range: "10.0.1.0/24"
private_ip_google_access: true
enable_flow_logs: true
secondary_ip_ranges:
  services:
    ip_cidr_range: "192.168.0.0/20"
# IAM configuration
